class JournalEntry {
  final int id;
  final String title;
  final String body;
  final int rating;
  final DateTime dateTime;

  JournalEntry({this.id, this.title, this.body, this.rating, this.dateTime});
}
